{
  "$schema": "http://json-schema.org/draft-07/schema",
  "required": [
    "environment"
  ],
  "properties": {
    "environment": {
      "type": "string",
      "enum": ["local", "staging", "production"]
    }
  }
}
